{
  "Routes": [
    {
      // === Read All Route in Service
      "UpstreamPathTemplate": "/v1/graphql",
      // === Avaiable Method to Use
      "UpstreamHttpMethod": [ "Get", "Post", "Update", "Delete" ],
      // === Protocol
      "DownstreamScheme": "http",
      "DownstreamHostAndPorts": [
        {
          // === SERVICE HOST
          "Host": "192.168.18.153",
          // === SERVICE PORT
          "Port": 8080
        }
      ],
      "DownstreamPathTemplate": "/v1/graphql"
    }
  ],
  "GlobalConfiguration": {
    // === .NET Gateway URL
    "BaseUrl": "http://localhost:5145"
  }
}